# input allow users to input data in the program 

name=input("enter the patient name:")
age=input("enter year of birth:")
status = bool("are you new to the hospital?")

print("name: ",name,"age: ",2024-int(age),"status: ",status)
# we use int to convert a string into integer