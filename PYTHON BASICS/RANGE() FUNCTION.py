# range() function is used as for i in range()

numbers = range(5,10,2) # first digit represents start and second represents end and third represents the steps the loop will count
for number in numbers:
    print(number)